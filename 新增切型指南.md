# 新增切型指南 — gemcraft.html 圖紙擴充手冊

> 這份文件是**發包規格書**:給任何 AI 模型或人類,照著做就能為《異世界魔法石工坊》新增切割圖紙,
> 不需要讀懂整個 gemcraft.html。
> 前置知識:高中三角函數。不需要 Three.js 知識(除非你要動 UI)。

---

## 0. 系統一句話

石頭 = 凸多面體。每一刀 = 一個平面把多面體裁掉一塊(`clipSolid`)。
一份「圖紙」= 一串 tier,每個 tier = **一個角度 + 一個深度 d + 一組 index 齒位**。
遊戲照表逐刀切,最後 `auditDiagram` 拿成品每個切面的法線與平面距離對表評分。

**你要做的事只有兩件:在 `DIAGRAMS` 陣列加一筆資料、在選石畫面加一張卡。零演算法改動。**

---

## 1. 資料格式(加在 gemcraft.html 的 `DIAGRAMS` 陣列)

```js
{id:'asscher49', name:'八角階梯 · Asscher 49', fold:8, gear:96,
 desc:'49 刻面階梯切工:垂直腰刀塑八角 → 亭 55/42/33° 三階 · 冠 42/27° 兩階 · 桌面。',
 tiers:[
  {id:'G',  side:'girdle',   angle:90, d:0.90,  indices:[96,12,24,36,48,60,72,84]},
  {id:'P1', side:'pavilion', angle:55, d:0.754, indices:[96,12,24,36,48,60,72,84]},
  // ...
  {id:'T',  side:'crown',    angle:0,  d:0.37,  indices:[96], indexFree:true}]},
```

| 欄位 | 意義 |
|---|---|
| `id` | 圖紙唯一代號(英數,`__diag.run(id)` 用它) |
| `name` | 顯示名(中文 · 英名+面數 的慣例) |
| `fold` | 對稱折數(目前純標註,程式沒用到) |
| `gear` | 分度齒數,目前一律 `96`(見第 6 節) |
| `tiers[].id` | tier 代號(P1/C1/G/T…),顯示在指令表 |
| `tiers[].side` | `'pavilion'`(亭,不翻面)/ `'crown'`(冠,翻面)/ `'girdle'`(腰,垂直刀,不翻面) |
| `tiers[].angle` | 切割角度,0=水平桌面、90=垂直腰稜。**滑桿 step 0.5,只能用 0.5 的倍數** |
| `tiers[].d` | 平面深度,單位 ×R(R=預成形粗胚邊心距),絕對距離。**深度止停滑桿範圍 20–150(%R)** |
| `tiers[].indices` | 96 齒分度盤上的齒位列表(96=0°,每齒 3.75°) |
| `tiers[].indexFree` | 桌面專用:角度 0 時自旋不影響法線,`indices:[96], indexFree:true` |

**tier 順序 = 施工順序**,通常:腰刀(如果有)→ 亭部由陡到緩 → 冠部由陡到緩 → 桌面。

### 選石畫面加卡(同檔 `#diagramRow` 內)

```html
<button class="diagCard" data-diag="asscher49">
  <span class="dcEmoji">🔳</span>
  <span class="dcText">八角階梯 · Asscher 49<small>階梯切工 · 垂直腰刀 · 中階</small></span>
</button>
```
`data-diag` 對上 `DIAGRAMS` 的 `id` 就完事,點擊綁定是自動的。

---

## 2. 幾何模型(算 d 值必讀)

### 2.1 預成形粗胚(所有圖紙的起點)

「圖紙預成形」把原石裁成標準 24 角柱:
- **邊心距 = R**(d 的單位基準;24 角柱「側面平面」到軸心的距離)
- **腰圍平面 = y0**(局部座標原點高度)
- **亭部往下留 −1.05R,冠部往上只留 +0.55R** ← 最重要的預算限制

### 2.2 d 的公式

一個 tier 的切割平面,通過「半徑 r、離腰圍垂直距離 h」的那一圈點時:

```
d = r·sin(angle) + h·cos(angle)
```

- 亭部:h = 平面在該半徑處**低於**腰圍的深度(正值)
- 冠部:h = **高於**腰圍的高度(正值)——翻面後座標鏡像,公式同形
- 腰刀(angle=90):退化成 `d = r`,r = 目標邊心距
- 桌面(angle=0):退化成 `d = h`,h = 桌面高度

### 2.3 逐 tier 鏈式計算(worked example:Asscher 亭部)

每個 tier 從上一個 tier 的「斷點」接力。斷點 = 你決定這層切到哪個半徑為止。

```
起點:腰圍 r=0.90(八角腰刀切出的邊心距),留腰帶 h=0.03
P1 55°: d = 0.90·sin55 + 0.03·cos55 = 0.754
  → 斷點選 r=0.55,該處深度 y = (d − 0.55·sin55)/cos55 = 0.529
P2 42°: d = 0.55·sin42 + 0.529·cos42 = 0.761
  → 斷點選 r=0.25,y = (0.761 − 0.25·sin42)/cos42 = 0.799
P3 33°: d = 0.25·sin33 + 0.799·cos33 = 0.806
  → 底尖收攏檢查:軸心深度 = d/cos33 = 0.961 < 1.05 ✓(底蓋會被吃掉,culet 收成尖點)
```

冠部同樣算法,方向朝上,最後桌面 `d = 桌面高度`。

### 2.4 五條硬檢查(每份新圖紙都要過)

1. **底尖收攏**:最後一個亭 tier 的 `d/cos(angle) < 1.05`,否則預成形底蓋殘留(多一個表外切面,稽核扣分)。
2. **桌面預算**:桌面 `d < 0.55`(冠部只有 0.55R 高)。冠部中間 tier 的 `d/cosθ` 超過 0.55 沒關係(那層只切外圈環帶)。
3. **陡緩次序**:同側從腰圍往中心,角度**遞減**(陡的先切、只吃腰邊;緩的後切、收進中心)。反過來會整層落空或吃掉底尖/桌面——這是本專案踩過最多次的坑。
4. **每層都要切到東西**:一個 tier 的平面如果完全在剩餘石頭外面,那層 0 個切面,稽核 matched < total,一驗就露餡。
5. **腰刀頂點 < 1.0R**:相鄰兩刀腰刀的交角頂點 = `d/cos(相鄰刀夾角的一半)`,必須小於 1.0,否則與 24 角柱側面共面 → 退化幾何風險。八角(45°間隔)d≤0.90 → 頂點 0.974;三角(120°間隔)d≤0.49 → 頂點 0.98。

---

## 3. 96 齒的對稱規則(硬限制)

### 3.1 可用折數

96 = 2⁵×3,所以整除的折數:**2、3、4、6、8、12、16、24、32、48**。
- 8 折 → 齒位間隔 12(如 `[96,12,24,36,48,60,72,84]`)
- 16 折 → 間隔 6;兩組交錯:6 的倍數 vs 奇數×3(錯位半格=11.25°,Portuguese 用這招)
- 3 折 → 間隔 32(`[96,32,64]`)
- **5 折、7 折、9 折做不到**(96 不整除)→ 這就是 80 齒議題,見第 6 節

### 3.2 鏡像對稱(不能省)

冠部翻面會鏡像方位:齒位 t 變成 **96−t**。所以**每個 tier 的 indices 集合必須滿足:t 在集合裡 ⟺ 96−t 也在**。
- `[96,12,24,...,84]`:96−12=84 ✓
- `[3,9,15,...,93]`:96−3=93 ✓
- `[96,32,64]`:96−32=64 ✓
- 隨手寫 `[96,10,20]`:96−10=86 不在 → **冠亭對不上,禁用**

### 3.3 做不到的東西(別接這種需求)

- **拉長外形**(橢圓/梨形/馬眼/長方 baguette/祖母綠長方):需要同一 tier 各齒位**不同深度 d**(真機叫 cheater/CAM),現有「一 tier 一深度」的資料模型與深度止停 UI 裝不下。要做得改資料格式+止停邏輯+勾銷匹配,是功能開發不是加資料。
- **凹面/內孔/雕刻**:切割引擎是凸多面體,物理上只能凸形。
- **曲面**(真 trillion 的弧邊、糖麵包山):同上,只能用密集平面近似。

---

## 4. 校準與驗收流程(必跑,不跑不算完成)

1. 打開 `gemcraft.html#dev`(**改 hash 後要手動重新整理**,hash 變更不會重載腳本)。
2. Console 執行 `__diag.run('你的圖紙id')` — 自動預成形+照表切完。
3. 看輸出的刻面數是否等於預期(表定面數 + 殘留的 24 角柱腰帶面數;有腰刀的話腰帶被取代)。
4. 執行 `auditDiagram()` → 必須 `matched === total`、`worst.dev` 接近 0。
   - matched 少了 = 某層沒切到東西(檢查 2.4 第 4 條)或斷點算錯。
5. 目檢 3D 視圖與右上導覽器(俯視):外形輪廓對不對、腰帶還在不在、底尖有沒有收攏、有無破面毛邊。
6. `__diag.dump()` 可傾印每個切面的實測角度/方位/d(%R),跟表對照除錯。
7. 走一次玩家路徑:選圖紙 → 圖紙預成形 → 點 tier 列 → 魔法陣列快切 → 每層打勾 → 提交結算。
8. 回歸:把**既有全部圖紙**也 `__diag.run` 一遍,確認沒被波及(改的是資料就不會,但跑一遍便宜)。

---

## 5. 資料來源與授權

- 真實圖紙庫:`gemologyproject.com/wiki/index.php?title=Faceting_Designs`。
  注意 wiki 頁面本身**沒有角度表**,數據在各設計頁附的 PDF / GemCAD(.gem)檔裡,要手工轉錄。
- 轉錄時注意作者授權:部分作者(如 Surgical Precision Gems)有明確使用限制,轉錄前看各頁條款。
- 真實圖紙的深度通常以「腰圍百分比/W%」或 GemCAD 內部單位表示,**不能直接抄**,要用第 2 節公式換算成本專案的 d(×R),再照第 4 節校準。
- 自創圖紙(像 Asscher 49/Portuguese 97/Trillion 16 就是本專案自創的簡化版)沒有授權問題,角度取經典切工的常見範圍即可。

---

## 6. 80 齒擴充難度分析(要做 5 折對稱才需要)

**動機**:5 折(五芒星/花式)需要齒數整除 5,96 不行,真機用 80 齒 gear(80=2⁴×5,支援 2/4/5/8/10/16/20 折)。

### 6.1 難度分級

| 等級 | 內容 | 工作量 |
|---|---|---|
| **A. 資料層(容易)** | `DIAGRAMS` 已有 `gear` 欄位,新圖紙標 `gear:80` | 已預留,零成本 |
| **B. 核心數學(中等,集中)** | 所有 `/96`、`%96`、`*3.75` 的硬編碼參數化成 `gear`。已知位置:`expectedNormal`(i/96)、`setIndex` 迴繞、`recordDiagramCut`(%96)、`nextTargetIndex`、`updateCompassTargets`((t%96)*3.75)、折數快捷鈕(step=96/fold)、`buildCompass` 度數環、鏡像對稱檢查(t→96−t 變 t→gear−t) | 半天,grep `96` 逐一判斷是齒數還是別的 |
| **C. UI(中等)** | index 錶盤/羅盤刻度重畫成 80 格;折數快捷鈕集合要按齒數切換(96:8/16/4/6 → 80:8/16/5/10);指令表與方位顯示的度數換算(80 齒每齒 4.5°) | 半天 |
| **D. 機台視覺(麻煩,可擺爛)** | 內嵌 GLB 的分度輪是**實體 96 齒**建模,80 齒圖紙會看到齒數對不上。要嘛重跑 `machine_model/build_machine.py` 生一顆 80 齒輪(再 `inject_glb.ps1`),要嘛接受視覺不同步 | 重生模型一輪 ~1 小時;擺爛 0 成本 |
| **E. 存檔相容(小心)** | Vault 縮圖與參數記錄了 index 值,舊記錄全是 96 齒語意。加 `gear` 進 `stoneRecordFromState`,詳情頁顯示時帶單位 | 1-2 小時 |

### 6.2 建議路徑

1. 先做 B:引入 `const GEAR = state.diagram?.def.gear ?? 96`,把硬編碼全參數化,**96 齒行為完全不變**(回歸:全部既有圖紙 `__diag` 重掃)。
2. 再做 C 的最小版:錶盤刻度與折數鈕跟著 GEAR。
3. D 直接擺爛(視覺 96 齒照轉),E 加欄位。
4. 最後才寫第一份 80 齒圖紙(建議五折星形 step cut 當試金石)。

**總估**:對熟悉此 codebase 的模型約 1–2 個工作 session;風險集中在 B 的漏網硬編碼——驗收就是「96 齒全圖紙回歸 + 80 齒新圖紙稽核 100%」。

### 6.3 預成形不用改

24 角柱粗胚與齒數無關(24 跟 80 不整除也沒關係——腰帶本來就會被腰刀或密集刻面取代/覆蓋),`preformDiagram` 一行不用動。5 折圖紙想要五邊腰圍,用 `side:'girdle'` 5 刀塑形即可(d/cos36° < 1.0 → d ≤ 0.80)。

---

## 7. 交付檢查清單(發包驗收用)

- [ ] `DIAGRAMS` 新條目:angle 全是 0.5 倍數、d 在 0.20–1.50、indices 鏡像對稱、tier 順序=施工順序
- [ ] 選石畫面 `#diagramRow` 有新卡,`data-diag` 對得上
- [ ] `#dev` 稽核:matched === total,worst.dev ≈ 0
- [ ] 目檢:輪廓正確、底尖收攏、無破面、腰帶(或腰刀面)存在
- [ ] 玩家路徑走通:預成形 → tier 帶入 → 陣列快切 → 勾銷 → 結算
- [ ] 既有圖紙全部回歸通過
- [ ] `claude.md` 第 3 節補一行記錄
